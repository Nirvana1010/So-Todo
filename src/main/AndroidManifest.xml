<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vodka.sotodo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_CALENDAR"/>
    <uses-permission android:name="android.permission.WRITE_CALENDAR"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".PagesClass" />
        <activity android:name=".moreInfo" />
        <activity android:name=".MainAct" />
        <activity android:name=".TimeShow"/>
        <activity android:name=".TimeManagement" />
        <activity android:name=".TimeCount" />
        <activity android:name=".pomodara" />
        <activity android:name=".SecActivity"/>
        <activity android:name=".TirdActivity"/>
        <activity android:name=".FirstActivity"/>

    </application>

</manifest>